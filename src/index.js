import React from "react";
import ReactDOM from "react-dom";
import style from "./style.scss";

const App = () => {
  return <div>
    <h1>React, Babel, Webpack, Sass boilerplate</h1>
    </div>;
};

ReactDOM.render(<App />, document.querySelector("#root"));